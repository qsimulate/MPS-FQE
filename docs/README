With sphinx installed, documentation can be generated with:
```
make html
```

`.rst` files can be regenerated by running the following command from this directory:
```
sphinx-apidoc -o source ../mps_fqe
```
